# Patterns

Useful repeatable shapes:

- problem -> evidence -> decision -> update
- capture -> classify -> compact
- request -> smallest artifact -> verify
- rough note -> mined signal -> shared doctrine
- doctrine change -> orchestrated test -> before/after result

Patterns should help an agent move faster on the next pass.
