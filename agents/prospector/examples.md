# Prospector Examples

- "Save -> Read -> Verify" shows up in multiple places, so it becomes shared doctrine.
- A recurring fix for stale prompts becomes a prompt phrase.
- Three separate notes about the same failure become one pattern.
