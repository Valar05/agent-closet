# Quartermaster Examples

- A repeated handoff problem becomes `shared/patterns.md`.
- A stable repo rule becomes `shared/doctrines.md`.
- A team-specific usage note becomes the agent pack that needs it.
- A confusing phrase gets rewritten once in `shared/prompt-phrases.md` and reused everywhere.
